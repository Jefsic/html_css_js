! + TAB (insere código básico de uma página HTML)
HTML:5 (insere o código básico de uma página HTML)
CTRL + ; (insere comentários na linha de código <!--  -->) (NO HTML)


ALT SHIF + seta para baixo (duplica linha)
seleciona a TAG e tecla CRTL + D (seleção multipla)
TAG + TAB (autopreenchimento da TAG)
CTRL + S (salva documento)
SHIF + ALT + F (identar código)
ALT + L  ALT + O (executa o código no navegador)

// insere comentários no java script
/* e */  (o que estiver no intervalo de linhas vira comentário

LOREN + TAB (preechimento com texto em latin para teste)

## Links adicionais
Questão da tag "u"
- https://developer.mozilla.org/en-US/docs/Web/HTML/Element/u


bash:
Executando o servidor
=====================
Com o nosso banco de dados fake, vamos acessar o arquivo package.json e mudar um de seus scripts. 
Atualmente, temos nele apenas um script de teste, mas não vamos trabalhar com testes nessa aplicação.
Então, vamos mudar o nome do script de test para start, porque queremos adicionar nele um comando para rodar a nossa aplicação back-end.

Esse comando será o json-server --watch db.json --port 3000.
Com esse comando, estamos dizendo que a biblioteca que instalamos, o JSON Server, 
vai ficar assistindo (watch) às modificações que vão acontecer no arquivo db.json, 
onde estão contidos os nossos dados. Além disso, dissemos que essa API vai rodar na porta 3000.
Com esse comando configurado no back-end, só vamos precisar digitar no terminal o comando
npm start
para subir o back-end. 
Senão, precisaríamos toda vez digitar todo esse comando, que é muito grande.
